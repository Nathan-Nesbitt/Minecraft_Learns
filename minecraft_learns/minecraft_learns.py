"""
    Example of basic workflow on minecraft_learns server. Each function will
    be triggered by messages that are send by the client. 
"""

# data = Data("src/foo.json")

# cleaned_data = Preprocess(data, x_column, y_column)

# model = Model(cleaned_data, model_type)

# trained_model = Train(model)

# prediction = Predict(trained_model, new_data)
